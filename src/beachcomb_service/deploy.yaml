apiVersion: apps/v1
kind: Deployment
metadata:
  name: beachcomb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: beachcomb
  template:
    metadata:
      labels:
        app: beachcomb
    spec:
      containers:
      - name: beachcomb
        image: studenten/beachcomb:$LATEST_COMMIT_HASH
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        env:
        - name: MYSQL_USER
          valueFrom:
            secretKeyRef:
              name: dse24-secret
              key: MYSQL_USER
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: dse24-secret
              key: MYSQL_PASSWORD
        - name: MQ_USER
          valueFrom:
            secretKeyRef:
              name: dse24-secret
              key: MQ_USER
        - name: MQ_PASSWORD
          valueFrom:
            secretKeyRef:
              name: dse24-secret
              key: MQ_PASSWORD
        envFrom:
        - configMapRef:
            name: dse24-config
        readinessProbe:
          failureThreshold: 3
          httpGet:
            path: /
            port: 80
            scheme: HTTP
